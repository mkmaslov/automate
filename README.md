# Automate

This repository contains various automation scripts.
Scripts from `/src/` folder depend on custom functions, defined in `/build/shared.sh`,
which can be embedded into scripts using `/build/bundle.sh`,
resulting in files in `/dist/` folder.
Folder `/lib/` contains resources, such as dotfiles and config files.
